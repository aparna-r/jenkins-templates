pipeline {
  agent any
  stages {
    stage('acceptance test') {
      steps {
        echo 'run functional test'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy release to acc env'
      }
    }
  }
}